@model Produto

<div class="col-sm-4 col-md-4 col-lg-4">
    <p><img src="@Model.ImgUrl" width="300" height="300" /></p>
    <h3>@Model.Descricao</h3>
    <h4>Preço: @Model.ValorUnitario.ToString("c")</h4>
</div>